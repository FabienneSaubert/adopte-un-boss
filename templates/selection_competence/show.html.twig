{% extends 'base.html.twig' %}

{% block title %}SelectionCompetence{% endblock %}

{% block body %}
    <h1>SelectionCompetence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ selection_competence.id }}</td>
            </tr>
            <tr>
                <th>Coeff_competence</th>
                <td>{{ selection_competence.coeffCompetence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_selection_competence_index') }}">back to list</a>

    <a href="{{ path('app_selection_competence_edit', {'id': selection_competence.id}) }}">edit</a>

    {{ include('selection_competence/_delete_form.html.twig') }}
{% endblock %}
