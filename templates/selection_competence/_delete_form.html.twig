<form method="post" action="{{ path('app_selection_competence_delete', {'id': selection_competence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ selection_competence.id) }}">
    <button class="btn">Delete</button>
</form>
